#!/bin/bash

# Проверка, запущен ли скрипт от имени root
if [ "$EUID" -ne 0 ]; then
  echo "❌ Пожалуйста, запустите скрипт с правами root (sudo)."
  exit 1
fi

echo "🚀 Начало обслуживания системы..."

# --- БЛОК ОБНОВЛЕНИЯ ---
echo "📦 Обновление списка пакетов..."
apt update

echo "⬆️ Обновление установленных пакетов..."
# Использование DEBIAN_FRONTEND=noninteractive помогает избежать диалоговых окон
DEBIAN_FRONTEND=noninteractive apt upgrade -y

# --- БЛОК ОЧИСТКИ ---
echo "🧹 Удаление неиспользуемых пакетов и конфигов..."
apt autoremove --purge -y

echo "🗑 Очистка кэша пакетов..."
apt autoclean -y
apt clean -y

# --- БЛОК ЛОГОВ И МУСОРА ---
echo "📜 Очистка системных логов (оставляем последние 7 дней)..."
journalctl --vacuum-time=7d

echo "✅ Обслуживание системы успешно завершено!"
echo "💾 Свободное место на диске:"
df -h / | grep /
